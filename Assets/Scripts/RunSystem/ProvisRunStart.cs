using UnityEngine;

public class ProvisRunStart : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        RunManager.Instance.StartRun();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
